# @package _global_
# Runtime options for the process script

defaults:
 - /runtime/base
 - /collect: surface_z_ur_Silvia
 - /sensor: tactip_dome127_ur_Silvia
 - _self_

settings:
  path: Y:/tactip-time-series-data/tactile_data

collect:
  name: surface-z-June26-Silvia
  data_dirs: [""]
  experiment: ${collect.robot}/${sensor.name}/${collect.name}

globals:
  REFERENCE: null 

sensor:
  process_images: null
  # process_images:
  #   visualise_on: true

  process_markers: # null
    visualise_on: true
    debug: true
    process_graph: null


